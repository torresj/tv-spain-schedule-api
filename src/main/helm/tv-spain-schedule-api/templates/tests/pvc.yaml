apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv-spain-schedule-api-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
