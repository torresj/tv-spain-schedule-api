#Loggin
logging:
  config: classpath:logback-spring-test.xml

spring:
  # H2 Database
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test
    driver-class-name: org.h2.Driver